import React from "react";
import ContractsList from "./ContractsList";

const HallContracts = (props) => (
  <ContractsList {...props} title="قرارداد های سالن عقد" />
);

export default HallContracts;
